/*
// Criando uma const sem um valor (inicializador):

const numero; // Const não pode ser cirado sem valor (inicializador).

*/

/*
// Nao fechando o parenteses ao final da declaração:

const numero = 1;
console.log(numero // missing ) after argument list.
*/

// Chamando uma variavel ainda não declarada:

const numero = 1;
console.log(minhaVar);
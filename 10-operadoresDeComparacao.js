// == (comparação implicita). Só compara o valor.

const numero = 245;
const texto = "245";
console.log(numero == texto);

// === (comparação explicita). Compara o valor e o tipo de dado. Mais utilizada por boas práticas.

console.log(numero === texto);

// typeof

console.log(typeof numero);
console.log(typeof texto);